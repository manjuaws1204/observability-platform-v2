name: Observability CI/CD

on:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Validate Environment
        shell: powershell
        run: |
          .\scripts\validate.ps1